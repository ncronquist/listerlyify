<% include ../partials/header %>

<div class="text-center">
  <h1>Listerlyify</h1>
  <h4>Make Twitter Lists Awesome</h4>
</div>

<div class="row">
  <div class="col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1">
<!--     class="display" width="100%" cellspacing="0"-->
    <table id="listgrideditor" class="table table-striped table-hover table-condensed" width="100%" cellspacing="0">
      <thead>
        <tr>
          <th></th>
          <% lists.lists.forEach(function(list, idx, array) { %>
            <th><%=list.name%></th>
          <% }) %>

        </tr>
      </thead>

      <tbody>
        <% friends.users.forEach(function(friend, idx, array) { %>
          <tr>
            <td><img src="<%=friend.profile_image_url%>" class="img-responsive" alt="Image"><%=friend.screen_name%></td>
            <% for(var i = 0; i < lists.lists.length; i++) { %>
              <td>x</td>
            <% } %>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<!-- Local JS -->
<script src="/js/listeditor.js"></script>



<% include ../partials/footer %>
